/*import React from 'react';
import styles from '../styles/Button.module.css';

type Props = {
    name: string;
    onClick?: (() => void);
};

function Button({onClick, name} : Props) {
    return (
        <button 
            className={styles.button} 
            onClick={onClick}>
            {name}
        </button>
    );
}

export default Button;*/